### Despliegue (React + Vite — hosting estático)

Producción vía build `dist/` en Vercel, Netlify o equivalente.

- [ ] CI GitHub en verde para el commit de `main` de esta versión
- [ ] Variables `VITE_*` nuevas cargadas en el hosting
- [ ] Build de producción exitoso
- [ ] SPA fallback / rewrites verificados si hay rutas cliente nuevas

### Validación post-despliegue

- [ ] Smoke: rutas principales, formularios, auth si aplica
- [ ] Consola del navegador sin errores nuevos en flujos tocados
